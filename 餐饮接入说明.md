# 餐饮 SaaS 消费者小程序接入说明（基于 yudao-mall-uniapp）

本目录的 `yudao-mall-uniapp` 是芋道官方商城 uni-app（**Vue3 + Sheep 框架**）。餐饮消费者端按 Sheep 约定接入，复用其请求封装、登录态、Pay 支付链路，**无需改动框架代码**。

## 1. 新增 / 改动文件清单

### 新增：API 模块（`sheep/api/restaurant/`）
由 `sheep/api/index.js` 的 `import.meta.glob('./<模块>/*.js')` 自动扫描聚合，引用方式 `sheep.api.restaurant.xxx`：

| 文件 | 对象 | 对接后端接口（前缀 `/app-api`） |
|---|---|---|
| `dish.js` | `RestaurantDishApi` | `GET /member/dish/category-list`、`GET /member/dish/simple-list` |
| `order.js` | `RestaurantOrderApi` | `POST /member/order/create`、`GET /member/order/get`、`GET /member/order/page`、`POST /member/order/pay-balance`、`POST /member/order/pay-weixin` |
| `coupon.js` | `RestaurantCouponApi` | `GET /member/coupon/my-list`、`POST /member/coupon/receive` |
| `member.js` | `RestaurantMemberApi` | `POST /member/me/bind`、`GET /member/me/get` |

### 新增：页面（`pages/restaurant/`，已注册到 `pages.json` 的 subPackage）
| 页面 | 路由 | 说明 |
|---|---|---|
| `menu.vue` | `/pages/restaurant/menu` | 扫码点餐（左分类右菜品 + 底部购物车），`onLoad` 解析小程序码 `scene=storeId=1&tableId=12` |
| `order-confirm.vue` | `/pages/restaurant/order-confirm` | 确认订单 + 微信/余额支付选择 |
| `order-detail.vue` | `/pages/restaurant/order-detail` | 订单详情 + 待支付可重新拉起支付 |
| `order-list.vue` | `/pages/restaurant/order-list` | 我的餐饮订单列表 |
| `member.vue` | `/pages/restaurant/member` | 会员中心（积分/成长值/我的券） |

### 改动：`sheep/config/index.js`
新增 `appKey`（餐饮 PayApp 的 appKey），并在默认导出里带上。页面用 `import { appKey } from '@/sheep/config'` 引用。

> 旧版独立骨架 `../../miniapp-consumer/` 是未对齐 Sheep 的 TS 推测版，**已弃用**，请以本工程内 `sheep/api/restaurant/` + `pages/restaurant/` 为准。

## 2. 关键约定（与后端对齐）

- **请求前缀**：Sheep 的 `baseURL = baseUrl + /app-api`（见 `.env` 的 `SHOPRO_API_PATH`）。芋道 nginx 只转发 `/admin-api`、`/app-api`，其余路径不转发——餐饮消费者接口已落在 `/app-api`，正确。
- **鉴权**：`sheep/request` 自动注入 `Authorization`（token 存 `uni.getStorageSync('token')`）、`tenant-id`（`.env` 的 `SHOPRO_TENANT_ID`）。消费者端 userType=MEMBER，与 `/app-api` 匹配。
- **金额单位**：前后端统一 **分（Integer）**，页面展示用 `price / 100` 转元。
- **会员档案**：餐饮会员（`restaurant_member`）与芋道 user 体系独立。进入「会员中心/确认订单」时先调 `member/bind` 自动 `getOrCreateMember`，余额支付需传返回的 `memberId`。

## 3. 微信支付链路（两步，已封装在 `RestaurantOrderApi.payWeixin`）

1. 前端调后端 `POST /member/order/pay-weixin?orderId&appKey&userId&userIp` → 后端生成芋道 Pay 订单，返回 `payOrderId`。
2. 前端用 `payOrderId` 调芋道 Pay 标准预支付 `POST /pay/order/submit`（Sheep 已有 `PayOrderApi.submitOrder({ id, channelCode:'wx_lite' })`）→ 返回 `displayMode='wx_lite'`、`displayContent` 为 JSON（含 `timeStamp/nonceStr/packageStr/signType/paySign`）。
3. 把 `packageStr` 改名 `package` 后调 `uni.requestPayment` 拉起微信收银台。

支付结果由后端 `OrderPayNotifyController`（`/admin-api/restaurant/order/pay/notify`）接收，前端可不轮询。

## 4. 配置点（上线前必填）

`.env`（dev 环境用 `SHOPRO_DEV_*`）：
- `SHOPRO_DEV_BASE_URL`：yudao-server 地址，如 `http://192.168.x.x:48080`
- `SHOPRO_API_PATH`：保持 `/app-api`
- `SHOPRO_TENANT_ID`：当前租户 id（多租户餐饮，每个商户一个租户）
- `SHOPRO_RESTAURANT_APP_KEY`：芋道后台【支付管理 - 应用信息】中**餐饮 PayApp** 的 appKey（新增项，原 `.env` 无，需补）

芋道后台还需配置（详见 `M0-code/README.md` 上线必配）：
- 【微信管理 - 微信小程序】社交客户端（wx_*），用于 `weixin-mini-app-login`
- 【支付管理 - 应用信息】餐饮 PayApp，并把**业务通知地址**配为 `http://<host>:<port>/admin-api/restaurant/order/pay/notify` 与 `/refund/notify`

## 5. 联调 checklist

- [ ] 后端 `mvn -pl yudao-module-restaurant -am compile` 通过
- [ ] `M0-code/sql/all_in_one.sql` 已执行建库
- [ ] `.env` 四项已填（baseUrl / apiPath / tenantId / restaurantAppKey）
- [ ] 微信开发者工具导入本工程，能登录（走芋道现成 `/member/auth/weixin-mini-app-login`）
- [ ] 进入 `/pages/restaurant/menu` 能拉到分类与菜品
- [ ] 下单 → 微信支付 → 后端回调把订单置已支付

## 6. 后续可扩展
- 门店端（admin 账号，走 `/admin-api/store-auth/*`）接入 `yudao-ui-admin-vue3`（已克隆到 `../yudao-ui-admin-vue3`）。
- 接桌台管理、加菜、接单等门店操作页面。
- 我的券详情 / 领券中心对接 `RestaurantCouponApi`。
